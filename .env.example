# Database
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Payload CMS
PAYLOAD_SECRET=           # long random string, used to sign auth tokens

# App
NEXT_PUBLIC_SERVER_URL=   # e.g. https://yourdomain.com (used for OG image absolute URLs)
NODE_ENV=development

# Exchange rate (ARS per 1 USD) — update manually as needed
ARS_PER_USD=1160
